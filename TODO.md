# TODO

- [x] Implement Artist “Play All” button in `app/artists/[id]/sections/Artist.tsx` by wiring it to music queue and starting playback.
- [x] Refactor `app/artists/[id]/stream/ArtistStream.tsx` so `ArtistInfo` receives `Songs`.
- [x] Ensure button works even if no songs are available.
- [x] Fix Playlist “Play All” button similarly (ensure it actually plays).
- [ ] Run Next/TypeScript check (build or lint) to confirm no TS errors.
